let value = 12; 
let negvalue = -value; // Correct way to negate the value
console.log(negvalue); // Output: 12 (original value is unchanged)

// Concatenating strings
// let str1 ="hello" // No need for parentheses when assigning a string
// let str2 = " dheeraj" // No need for parentheses here either

let str3 = str1 + str2; // Concatenation of two strings
console.log(str3); // Output: "hello dheeraj"

 operations
console.log(2 + 2); // Addition: Output: 4
console.log(2 - 2); // Subtraction: Output: 0
console.log(2 * 2); // Multiplication: Output: 4
console.log(2 ** 3); // Exponentiation (2 to the power of 3): Output: 8
console.log(8 / 2); // Division: Output: 4
console.log(5 % 10); // Modulus (remainder of 5 divided by 10): Output: 5




// Uncommented concatenation and arithmetic operations
console.log( 1 +2 ); // This results in "12" because 1 is concatenated with the string "2"
console.log( 1+ 2); // This results in "12" because the string "1" is concatenated with the number 2

console.log( 2 + 2); // This results in "122" because "1" + 2 gives "12", then "12" + 2 gives "122"
console.log(1 + 1 + 3 ); // This results in "22" because 1 + 1 is 2, then 2 is concatenated with "2"

console.log(+true);
// code should be easy not tricky

let num1, num2 , num3
num1 = num2 = num3 = (2+2) 
// code should be simple 

let gamecounter = 100
(++gamecounter)
console.log(gamecounter);

// // some code is not running because of newer version of js 
// so i had just comment out that code 











